// Function expression called divide
const divide = (number) => (number / 100)
console.log(divide(2000))

//Function called square
const square = (squareNumber) => (squareNumber ** squareNumber)

//adds two parameters together
const add = (num1, num2) => (num1 + num2)